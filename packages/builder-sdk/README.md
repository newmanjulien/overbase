# @overbase/builder-sdk

This package is intended to become the external shared SDK for builder app manifests, host protocols, and runtime helpers.

During early development it is vendored locally into the Overbase and Bring the Firm repositories. Keep the local copies in sync when changing shared contracts.
